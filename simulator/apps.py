"""Application configuration for the simulator app."""

from django.apps import AppConfig


class SimulatorConfig(AppConfig):
    """Configuration for the simulator Django app."""

    name = 'simulator'
